#!/bin/bash 
dir=$(cd $(dirname $BASH_SOURCE) && pwd)
cmd=$1
shift
case $cmd in
    fanyi) cd $dir && ./fanyi $@;;
    ctime) python -c "import time; print time.ctime($@)";;
    *) echo unkown command;;
esac
